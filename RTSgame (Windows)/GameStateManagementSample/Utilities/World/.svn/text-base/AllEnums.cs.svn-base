using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace RTSgame.Utilities.World
{
    enum SquareTerrainType : byte { None, Grass, Mud, HighGrass, Hill, Forest, Mountain, BorderMountain, ScatteredTree, MuddyOpen };
    enum LargeAreaType : byte { None, ThickForest, MountainComplex, Pit, Hills, ProtectedGrass, ScatteredTrees, MuddyOpen };

    [Flags]
    enum TextureManipulation : byte
    {
        None = 0x0,
        HorizontalSwap = 0x1,
        VerticalSwap = 0x2,
        DiagonalSwap = 0x4
    }
}
