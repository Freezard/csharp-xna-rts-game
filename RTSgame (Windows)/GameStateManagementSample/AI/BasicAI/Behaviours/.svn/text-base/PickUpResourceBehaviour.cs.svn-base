using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using RTSgame.GameObjects.Abstract;
using RTSgame.GameObjects.Buildings;
using RTSgame.GameObjects;
using RTSgame.GameObjects.Economy.Zones;
using RTSgame.AI.BasicAI.Priorities;

namespace RTSgame.AI.BasicAI
{
    //A special type of chase behaviour where minions move to resourcepickupzones
    class PickUpResourceBehaviour//:ChaseBehaviour<Minion, ResourcePickUpZone>
    {/*
         public PickUpResourceBehaviour(Priority<Minion> priority, float speedScale)
            : base(priority, speedScale,0)
        {

        }
         public override bool FulfilCriteria(Minion me, IInteractable otherGameObject)
        {
            if (base.FulfilCriteria(me, otherGameObject))
            {

                    return !me.IsCarryingResource() && ((ResourcePickUpZone)otherGameObject).IsActive();

                //return ((PlayerCharacter)otherGameObject).Follow();
            }
            else if (otherGameObject is MetalResource)
            {
                return !me.IsCarryingResource();
            }
            else
            {
                return false;
            }
        }*/
    }
}
