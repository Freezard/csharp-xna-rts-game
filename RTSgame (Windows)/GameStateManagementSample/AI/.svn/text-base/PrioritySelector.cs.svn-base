using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using RTSgame.GameObjects.Abstract;
using RTSgame.AI.Abstract;

namespace RTSgame.AI
{
    class PrioritySelector<T> : Selector<T> where T : IIntelligent
    {
        
        //PrioritySelectors should select from many different options with different priorities, but right now all it does
        //is being a "custom" selector
        public PrioritySelector()
        {
          

        }

        public override void choose(AIManager<T> aiManager)
        {
            //List<GameObject> surroundings = aiManager.GetSurroundings();
            throw new NotImplementedException();
        }


    }
}
